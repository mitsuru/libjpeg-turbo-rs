# Last Mile Replacement Gate — Index

> **Purpose:** the release gate for "can `libjpeg-turbo-rs` replace C libjpeg-turbo as a system library?" — Rust-native APIs, TurboJPEG/TJ3 C ABI, classic `jpeg_*`, stock tools, downstream wrappers.

> **For agentic workers:** read this index first. Open the phase file(s) below ONLY for the items you are working on. Do not load all phases by default.

**Tech stack:** Rust 2021, `libjpeg-turbo-rs`, `libjpeg-turbo-rs-capi`, upstream sources in `references/libjpeg-turbo`, Homebrew/system `djpeg`/`cjpeg`/`jpegtran`, Pillow/ImageMagick smoke harnesses.

---
## Current Status (2026-08-08)

Project replacement readiness is tiered (this list is the canonical T1–T4 framing; [README.md](../README.md#c-abi-replacement-tiers) points here):

- **T1.** Rust crate (`use libjpeg_turbo_rs::*;`) — **ready** today.
- **T2.** TurboJPEG cdylib (`libturbojpeg.so.0`) — **ready** for TJ3 consumers (opaque-handle API, no struct ABI risk). Legacy 1.x/2.x surface is partial: **21 legacy aliases wired** in `crates/libjpeg-turbo-rs-capi/src/legacy.rs` (lifecycle `tjInitCompress`/`tjInitDecompress`/`tjInitTransform`/`tjDestroy` + `tjCompress2`/`tjDecompress2`/`tjDecompressHeader3` + `tjTransform`/`tjEncodeYUV3`/`tjDecodeYUV` + buffer-size helpers + `tjLoadImage`/`tjSaveImage` + `tjGetErrorStr2`); **18 deliberately deprecated** (v1 / un-versioned variants like `tjAlloc`, `tjFree`, `tjCompress`, `tjGetScalingFactors`). P4-18 closed 2026-05-19 via Option B — `docs/ABI_COMPATIBILITY.md` carries a per-symbol migration matrix and a tiny-shim recipe (`unsigned char *tjAlloc(int b) { return tj3Alloc((size_t)b); }` pattern) so consumers compiled against the missing 18 can either recompile against TJ3 or ship a one-translation-unit shim. Option A (wiring the 18 in our cdylib) remains tracked in P4-18's body for the case where a downstream consumer cannot ship the shim translation unit.
- **T3.** Classic libjpeg v8 cdylib (`libjpeg.so.8`) — **experimental/partial, not yet a general drop-in.** The shim defaults to the v8 SONAME, and the pinned OpenCV 4.6 workload proves real binding, encode/decode, and bidirectional interoperability for that measured default path. It does not cover the open P0/P1 ABI, state, option, error, and test-integrity gaps P4-84..P4-116 (the P0 destination-ownership gap P4-108 is closed; the create-guard P0 P4-110 is not); P4-81 separately tracks missing ELF symbol versions. Treat the harness as downstream evidence, not blanket compatibility.
- **T4.** System v6b/v7 drop-in (`libjpeg.so.62` / `libjpeg.so.7`) — **explicit non-goal** until per-ABI cdylib matrix ships. v6b is available only behind explicit `CAPI_SONAME=libjpeg.so.62 + CAPI_ACK_V6B_SONAME=1` opt-in.

Phase 3 history: P3-1 / P3-2 / P3-3 / P3-4 / P3-5 / P3-6 all CLOSED. P3-2 closed 2026-05-09 with the full 12-bit `jpeg_*_raw_data` backend wired through `libjpeg_turbo_rs::raw_data_12::{compress,decompress}_raw_12`. Phase 4 post-gate corrections: P4-1 exported `jpeg_calc_jpeg_dimensions` (closed 2026-05-10); P4-2 introduced the T1–T4 replacement-tier framing (closed 2026-05-17); P4-3 flipped the default C-ABI SONAME from `libjpeg.so.62` to `libjpeg.so.8` (closed 2026-05-17).

**Live gate** (refresh whenever the inventory changes):

| Check | Result |
| --- | --- |
| `cargo test --workspace --release` | **Passes** — 2372 tests, 0 failures, **0 ignored** (last re-measured 2026-07-28 on macOS aarch64 while closing #387, whose 3 crate-level doctests joined the P4-65 + P4-68 regression gates and the #382/#383/#384 set — the workspace gate cannot be re-run on a Windows host; see the P4-62 closure for the Windows CI leg that now builds all targets). The P4-74/75/76 closures added 9 more tests on 2026-07-30 (3 in `regression_scaled_decode_idct_overflow.rs`, 3 in `regression_lossless_partial_scan.rs`, 2 unit + 1 harness test), measured only as `2360 passed / 0 failed / 0 ignored` on a **Windows** host — that host total is not comparable to the macOS figure above (platform-gated tests differ), so the 2372 line stands until the next macOS run. P4-119 additionally adds 4 public-API integration tests, measured as `4 passed / 0 failed / 0 ignored` on this **Linux x86_64** refactor worktree; they are not folded into the macOS-qualified 2372 total. Every P4-39 (#313), P4-46 (#322) and P4-41 (#314 / #362) reproduction runs as a regression. |
| `cargo test -p libjpeg-turbo-rs --test cross_product_transform` | **Passes 12 tests** with exact accounting: every attempt is bucketed as round-tripped, refused, or a decode failure, and the buckets must sum to the attempt count (core 312/312, crop 240/240, restart 144/144, grayscale 120/120, arithmetic 112/112, trim 78/78, full 14,112/14,112). The former `Err(_) => { /* legitimate failure */ }` arms that counted refusals as successes are gone. P4-116 named-matrix scope closed 2026-08-08. |
| `cargo test -p libjpeg-turbo-rs --test regression_progressive_4pixel_chroma_transform` | **Passes** 256 cases byte-exact vs `jpegtran -progressive -copy all <op>`. P3-4 closed. |
| `cargo test --test cross_check_p3_6_nonstandard_rgb565` | **Passes** 4 fixtures: 3x2 decode (vs `djpeg`), 3x2 encode (vs `cjpeg -sample 3x2,1x1,1x1` + `djpeg`), 3x1 decode, RGB565 merged-upsample (vs `djpeg -nosmooth` + 5-6-5 truncate chain). P3-6 closed. |
| `cargo test --release --features full-c-parity --test c_tjtrantest c_tjtrantest_full` | **Passes** — 12,230 tested, 18,498 skipped (skip set covers unrelated tjtrantest exclusions, not P3-4). |
| `examples/stock_djpeg_cjpeg/run.sh` | `OK fixtures=N cases=5N` — every fixture is byte-exact vs stock djpeg and `jpegtran -copy all -rotate 90`; cjpeg is byte-exact or stock-decodes to identical output; COM round-trip and tjbench smoke also pass. P0-2 + P0-4 closed. |
| `cargo test --test capi_stock_tool_link` | **Passes** the five-operation-per-fixture stock-tool matrix plus stale/partial/oracle-contamination false-green regressions. |
| `cargo test --test capi_pillow_compat` | **Skips on a v6b-bundled Pillow, by design (P4-122).** The runner now refuses to overwrite a bundled `libjpeg*.so.62*` with the v8 shim: a stock manylinux wheel's `_imaging.so` links v6b and requests `jpeg_*@LIBJPEG_6.2`, so substituting bound a v6b consumer to a v8 struct layout and called it evidence. That is forbidden — T4 is a non-goal — and it only appeared to work because the shim declared no ELF symbol versions. Real T3 evidence needs a Pillow whose `_imaging.so` links v8; until that path exists this leg reports an explicit skip rather than a pass. P0-3's closure rests on the earlier source-built v8 run. |
| `cargo test -p libjpeg-turbo-rs-capi --test tjunittest_link` | **Passes** without `--include-ignored`. |
| `cargo test -p libjpeg-turbo-rs-capi --test abi_offsets --release` | **Passes** all 6 cross-checks: `jpeg_decompress_struct` (P2-4) + `jpeg_marker_struct` + `jpeg_compress_struct` + `jpeg_error_mgr` + `jpeg_source_mgr` + `jpeg_destination_mgr` against upstream `jpeglib.h` at `JPEG_LIB_VERSION=80`. P3-1 closed. |
| `cargo test -p libjpeg-turbo-rs-capi --test libtiff_integration --release` | **Passes** end-to-end libtiff COMPRESSION_JPEG round-trip via the cdylib (skips with reason if libtiff/cc are absent). The shim's `jpeg_read_header` walks markers manually to detect tables-only abbreviated datastreams (libjpeg.txt §6) and splices the cached prefix in front of each strip's body so `Decoder::new` parses the unified stream. Previously `#[ignore]`d as a known shim gap; un-ignored 2026-05-09. |
| C-ABI option/lifecycle binaries | `capi_jpeglib_encode`: **16/16** (14 stock-`cjpeg` option cases; lossless outputs exact via `djpeg`; two stale invalid-call smokes removed). `capi_classic_lifecycle_pathological`: **4/4**, no skips with host tools; PATH-aware required-oracle/compile failures are fatal in CI. |
| `cargo test -p libjpeg-turbo-rs-capi --test capi_classic_dest_ownership` | **Passes 12 C canaries, 0 skips** — `jpeg_mem_dest` caller-buffer ownership (stack/static/heap, growth, NULL buffer, NULL out-params, zero-capacity, reuse) and `jpeg_stdio_dest` I/O errors (short write from both `term_destination` and `empty_output_buffer`, `/dev/full` flush failure, success control). Differential in two layers: the same C binary is relinked against a reference v8 build via `LIBJPEG_TURBO_REFERENCE_DIR` and the contract reports must match exactly, while each canary also decodes its own output back to RGB and compares size + error against the reference within a factor of two — so a manager that dropped the entropy-coded payload cannot pass on `SOI…EOI` alone. P4-108 closed. |

---
## Replacement Gate (8-item checklist)

Do not call the project a libjpeg-turbo C replacement until all are true:

1. `cargo test --workspace --no-fail-fast` is green with no product-path ignored tests except explicitly slow release-only stress tests.
2. `cargo test --test capi_stock_tool_link -- --include-ignored` is green, or the ignored attributes are removed and the default run is green.
3. `cargo test --test capi_pillow_compat -- --nocapture` fails on blocker code 3 until fixed, then passes through real Pillow decode+encode.
4. `cargo test -p libjpeg-turbo-rs-capi --test tjunittest_link -- --include-ignored --exact tjunittest_default_suite_passes` is unignored and green in the normal suite.
5. Stock `djpeg`, `cjpeg`, `jpegtran` and real C harnesses cross-validate output plus public state/ownership/error contracts, with no open P0/P1 classic-ABI or test-integrity gaps.
6. The shim exports every symbol required by the linked stock tools and by the Pillow/ImageMagick smoke harnesses, including high-precision raw-data entry points.
7. Performance reporting is re-run after correctness is green. Correctness blockers take priority over decode/encode microbenchmarks.
8. On GNU/Linux, the candidate exports the reference-compatible ELF symbol-version nodes required by prebuilt consumers, and replacement runs produce no `no version information available` loader warnings.

Items 5 and 8 are not green. P4-84..P4-116 track the classic contract/test gaps; P4-81 tracks ELF versions. T3 is proven only for the measured workloads.

---
## Open Items

The table below is the live OPEN set across all filing dates. **P4-13 / P4-14 / P4-17** were filed 2026-05-18 after an independent cold review (Claude + codex, two-pass) surfaced four C-ABI fidelity gaps the prior Phase-4 closures missed and a falsifying flaw in the P4-5 closure's primary suspension test; **P4-17** (the real-suspension test it delivers) was closed 2026-06-02 and **P4-13** (incremental `jpeg_consume_input` suspension) landed **PARTIAL** the same day — its byte-exact-proven streaming-suspension core shipped, with three deeper streaming-contract gaps refiled as **P4-26** — leaving **P4-14** as the last fully-open original cold-review gap. **P4-20 / P4-21** followed from the IDCT i16-overflow (P4-19) and non-standard-sampling work. The 100k `fuzz_decode_diff_c` smoke sweep (seed 424242, `~/smoke100k/`) filed **P4-22** and **P4-23** on 2026-05-30; both were fixed and closed 2026-05-31. **P4-24** (an arithmetic-path sibling of P4-22, found in the P4-22 review) was filed 2026-05-31 and closed 2026-06-01; **P4-25** (a pre-existing per-scan DAC-snapshot gap surfaced in the P4-24 review) was filed 2026-06-01. **P4-26** (three deeper streaming-contract gaps from the P4-13 codex round-8 review — `read_header`-stop-at-SOS for all sources, output-driven input pull, incremental marker-list stability) was filed 2026-06-02. Scheduled Fuzz Smoke runs 27930557237 and 28349528808 filed **P4-27** (single-component baseline h1v4 block order) and **P4-28** (progressive AC-refine one-past-`Se` placement) on 2026-06-29; both were fixed and closed the same day. Scheduled Fuzz Smoke run 28921468958 filed **P4-29** (block smoothing read dummy iMCU-padding blocks as DC neighbors) on 2026-07-08; fixed and closed the same day. Fuzz Smoke runs 117, 118, and 121 filed **P4-30** (unsupported 12-bit sampling layout wrote past a component plane) on 2026-07-12; fixed and closed the same day. The 2026-07-19..24 scheduled Fuzz Smoke failures (12 runs, 4 targets) filed **P4-34**..**P4-38** on 2026-07-24; all five were fixed and closed the same day (P4-34 sparse-DQT-slot remap in `read_coefficients`; P4-35 category-16 coefficient rejection in the transcode writers + jdarith.c error-limbo port; P4-36 fractional-chroma-ratio reject matching C JERR_FRACT_SAMPLE_NOTIMPL; P4-37 SOS component-id binding per jdmarker.c get_sos; P4-38 lossless 0xFFFF undifference wrap + `<< Al` output scaler, byte-exact vs djpeg). **P4-31** (real-world/corpus gate hardening, the `v0.6.3` release blocker) was closed 2026-07-25: PR #307 delivered all six acceptance criteria and the required `Corpus Test (C parity)` job is green on post-merge runs. **P4-39** (CMYK silently drops encode options; GitHub [#313](https://github.com/developer0hye/libjpeg-turbo-rs/issues/313)) and **P4-40** (the `compress_*` copy-paste family that caused it) were filed 2026-07-25 from a structural review of `src/encode/pipeline.rs`. **P4-61** and **P4-62** were filed 2026-07-27 while adding the regression test for [#362](https://github.com/developer0hye/libjpeg-turbo-rs/issues/362) — a duplicate of #314 confirmed already fixed in v0.7.0: a CI job whose test filter matches nothing, and a Windows-host workspace build break. **P4-63** (adoption-gap umbrella, GitHub #380–#392) was filed 2026-07-27 as P4-55's adoption-focused twin — five defects plus packaging/presentation items found by a two-tree audit vs zune-jpeg. **P4-64** (lossless non-RGB→RGB-family conversion accepted where C raises `JERR_CONVERSION_NOTIMPL`) was filed 2026-07-28 after the #369 review's tj3 oracle run surfaced the divergence, and **P4-65** (12-bit grayscale decode ignores the requested output format) the same day from the #369 docs audit's check of P4-57's 12/16-bit acceptance criterion; P4-65 was fixed and closed 2026-07-28. **P4-68** (`decompress_to(.., Cmyk)` panics on non-CMYK sources where C raises `JERR_CONVERSION_NOTIMPL`) — fixed and closed 2026-07-28 in the same PR was filed 2026-07-28 by the #394 docs audit. The four consecutive 2026-07-29 scheduled Fuzz Smoke failures (runs 30420069849 / 30438301770 / 30461194331 / 30485530878, two targets) filed **P4-74** (reduced-size + extended IDCT kernels panic on `i32` overflow under scaled decode), **P4-75** (arbitrary-precision lossless decode indexes past `dc_tables` when `Ns < Nf`) and **P4-76** (the `fuzz_crashes.rs` replay harness dropped the fuzz targets' option matrix, so a committed crash seed sat green locally) on 2026-07-30; all three were fixed and closed the same day. **P4-77** (Windows-host clippy failures in the capi shim) was filed the same day from the pre-PR clippy run. **P4-78** (no AArch32 NEON backend — ARMv7 is the one mainstream platform where C vectorises and we do not, so the P4-60 scalar deficit multiplies with C's full NEON win) was filed 2026-07-30 from the #424 user question, which is also the concrete downstream request `phase2.md`'s 32-bit-ABI item named as its trigger. The remaining open items:

| ID | Heading | Phase | State | Note |
| --- | --- | --- | --- | --- |
| P4-13 | [`jpeg_consume_input` returns EOI instead of honoring per-byte source suspension](last_mile/phase4.md#p4-13-jpeg_consume_input-returns-eoi-instead-of-honoring-per-byte-source-suspension--partial) | 4 | PARTIAL | Streaming-suspension core landed + byte-exact-proven vs stock `djpeg` (suspending source → SOS/EOI/SUSPENDED lock-step, decode stays buffered). Deeper contract fidelity → P4-26. |
| P4-14 | [`max_memory_to_use` is ABI-mirrored but not enforced in the C-side allocation path](last_mile/phase4.md#p4-14-max_memory_to_use-is-abi-mirrored-but-not-enforced-in-the-c-side-allocation-path--open) | 4 | OPEN | Field at correct offset, but zero comparisons against it anywhere in `memmgr.rs`. |
| P4-20 | [x86 SSE2 IDCT full path is i32 4-lane, not an i16-faithful port](last_mile/phase4.md#p4-20-x86-sse2-idct-full-path-is-i32-4-lane-not-an-i16-faithful-port--open) | 4 | OPEN | Corrupt-input-only: rows-1–7-non-zero overflow blocks wrap/saturate unlike C-SSE2. AVX2 already faithful. Filed under the P4-19 fix. |
| P4-21 | [decoder rejects non-standard sampling where a chroma component out-samples luma](last_mile/phase4.md#p4-21-decoder-rejects-non-standard-sampling-where-a-chroma-component-out-samples-luma--open) | 4 | OPEN | `Cr=h3v1` > luma → upsample model assumes luma is max-sampled → `factor 0` reject where djpeg decodes. Colour-path refactor (A) or lenient recovery (B). |
| P4-25 | [arithmetic DAC conditioning is not snapshotted per scan](last_mile/phase4.md#p4-25-arithmetic-dac-conditioning-is-not-snapshotted-per-scan--open) | 4 | OPEN | All arith paths read the shared-global DAC params; a same-slot DAC redefinition between scans would mis-decode earlier scans. Pre-existing, found in the P4-24 review. NOT `fuzz_decode_diff_c`-reachable; standard `cjpeg` streams use disjoint slots. |
| P4-26 | [deeper streaming-contract fidelity beyond the P4-13 core](last_mile/phase4.md#p4-26-deeper-streaming-contract-fidelity-beyond-the-p4-13-core--open) | 4 | OPEN | Three upstream-contract gaps beyond the P4-13 core: (a) `read_header` stops at first SOS only on suspending sources; (b) buffered-image output calls don't pull source input; (c) `marker_list` rebuilt not extended (dangles retained `jpeg_saved_marker_ptr`). No known consumer; none block T3. From the P4-13 codex round-8 review. |
| P4-55 | [zune-jpeg competitive-gap program (#350–#361)](last_mile/phase4.md#p4-55-zune-jpeg-competitive-gap-program-350361--open) | 4 | OPEN | Umbrella for the 2026-07-26 zune gap analysis; detail lives in GitHub #350–#361. #350–#355 closed 2026-07-26/27 (see phase file Progress); #356–#359 + the #361 closing re-measurement remain. Includes the deliberate #355 parse-time scan-cap divergence from djpeg (no C library-level cap; oracle carve-out in fuzz_decode_diff_c). |
| P4-56 | [frame component cap is 4 where C accepts up to 10](last_mile/phase4.md#p4-56-frame-component-cap-is-4-where-c-accepts-up-to-10--open) | 4 | OPEN | `read_sof` rejects 5–10-component frames the C library handles via raw-data/coefficient paths (`jmorecfg.h` MAX_COMPONENTS=10; stock djpeg aborts at colour conversion, so the oracle is a C harness). Scan-level Ns ≤ 4 is exact parity. From the #351 docs audit. Low urgency. |
| P4-60 | [scalar kernels are ~2.5x slower than C's scalar kernels](last_mile/phase4.md#p4-60-scalar-kernels-are-25x-slower-than-cs-scalar-kernels--open) | 4 | OPEN | Measured on riscv64 where neither side has SIMD (#359 step 1). Affects RISC-V/POWER/s390x/32-bit ARM and every no_std build. Stable-Rust work, unlike portable-SIMD. Step 1 (table-driven YCbCr→RGB) landed 2026-07-28: kernel 1.63x, ours/C ~1.12x at 640x480 / ~1.72x at 1080p on that harness — the ~2.5x in the title is the pre-step-1 figure. |
| P4-59 | [Extended XMP writing not implemented](last_mile/phase4.md#p4-59-extended-xmp-writing-not-implemented--open) | 4 | OPEN | #358 landed read (incl. reassembly) + single-segment write; oversized packets error instead of chunking. |
| P4-64 | [lossless non-RGB decode accepts RGB-family conversions that C refuses](last_mile/phase4.md#p4-64-lossless-non-rgb-decode-accepts-rgb-family-conversions-that-c-refuses--open) | 4 | OPEN | C raises `JERR_CONVERSION_NOTIMPL` for lossless non-RGB→RGB-family output; we expand it (more-permissive). Surfaced by the #369 review's tj3 oracle; decision keep-vs-align pending. |
| P4-66 | [12-bit lossless undifference masks by precision where C wraps modulo 0xFFFF](last_mile/phase4.md#p4-66-12-bit-lossless-undifference-masks-by-precision-where-c-wraps-modulo-0xffff--open) | 4 | OPEN | `undifference_row_16` masks `(1<<precision)-1`; C's `jpeg_undifference*` wrap `& 0xFFFF` regardless of precision (P4-38's contract). Diverges on wrapping 12-bit/arbitrary-precision lossless input. From the #382 docs audit. |
| P4-67 | [`apply_orientation` strided reads are cache-hostile for orientations 5-8](last_mile/phase4.md#p4-67-apply_orientation-strided-reads-are-cache-hostile-for-orientations-5-8--open) | 4 | OPEN | ~12 KB source stride per pixel on a 4032 px-wide 12 MP RGB photo; a tiled transpose is estimated 3-6x faster on this shape (unmeasured). Perf-only — placement is pinned by the #391 tests. From the #391 review. |
| P4-69 | [`simd` feature contract and the remaining #389 safety-posture work](last_mile/phase4.md#p4-69-simd-feature-contract-and-the-remaining-389-safety-posture-work--open) | 4 | OPEN | Phase 1 (#389) landed deny(unsafe_op_in_unsafe_fn)+wraps, NEON feature-gating fixes, and a green Miri job; remaining: module-level feature enforcement, the ~780-site simd sweep, non-SIMD unsafe to zero, forbid(unsafe_code) goal. |
| P4-70 | [clippy structural-lint allow-list in test code](last_mile/phase4.md#p4-70-clippy-structural-lint-allow-list-in-test-code--open) | 4 | OPEN | #390 widened clippy CI to `--workspace --all-targets` with exactly three lints allowed in test code (56 sites on aarch64 / 47 on x86_64 — 43/34× needless_range_loop, 9× too_many_arguments, 4× type_complexity; host-dependent per cfg-gated test code); library code has zero allowances. Closes when the `-A` flags are deleted and the job stays green. |
| P4-71 | [scaled decode never dispatches to SIMD reduced-size IDCT on any arch](last_mile/phase4.md#p4-71-scaled-decode-never-dispatches-to-simd-reduced-size-idct-on-any-arch--open) | 4 | OPEN | The reduced-size 4/2/1 IDCT branches in `pipeline_impl/color.rs:181-207` always run scalar (8x8 blocks in a scaled decode still take SIMD): NEON `idct_4x4/2x2/1x1` ports exist but are never dispatched; x86_64/wasm have no ports (C ships `jidctred-sse2.asm`). From the #390 drift audit + codex. Benchmark-gated, SIMD backends only. |
| P4-77 | [Windows-host `cargo clippy --workspace` fails in capi/src/jpeglib.rs](last_mile/phase4.md#p4-77-windows-host-cargo-clippy---workspace-fails-in-capisrcjpeglibrs--open) | 4 | OPEN | Three `cfg(windows)`-only errors (2 unused imports, 1 unused `file` arg in `read_c_file`) break both CI clippy gates on a Windows host. Clippy job is ubuntu-only, so CI is unaffected. Check whether the Windows `read_c_file` arm is also functionally incomplete before silencing. Clippy twin of P4-62. |
| P4-78 | [no 32-bit ARM (AArch32) NEON backend — ARMv7 is our widest gap vs C](last_mile/phase4.md#p4-78-no-32-bit-arm-aarch32-neon-backend--armv7-is-our-widest-gap-vs-c--open) | 4 | OPEN | We dispatch 100% scalar on `target_arch = "arm"` while C compiles its 13 shared `simd/arm/*-neon.c` kernels for 32-bit too — so unlike RISC-V/POWER (scalar on both sides) the P4-60 deficit multiplies with C's whole NEON win. Inferred **2-5× slower**, no hardware measurement yet. `core::arch::arm` NEON intrinsics are unstable (`stdarch_arm_neon_intrinsics`, measured on 1.93.1), so hand-written kernels need `asm!` — but that blocks intrinsics only, **not auto-vectorisation**: the target baseline's `-neon` is what switches LLVM's vectoriser off (`+neon` takes `idct_8x8` from 0 to 270 vector instrs, 204/204 still green). Filed as option (D), explicitly **not** a default — compile-time flag ⇒ SIGILL on NEON-less cores, and unmeasured on silicon. Correctness gated by `armv7.yml` (criterion 5 delivered 2026-07-30); the perf gap stays OPEN. From #424; fires phase2.md's 32-bit-ABI trigger. |
| P4-80 | [public low-level grayscale override lacks source metadata and upsampling policy](last_mile/phase4.md#p4-80-public-low-level-grayscale-override-lacks-source-metadata-and-upsampling-policy--open) | 4 | OPEN | `decode_with_colorspace_override` is public and must retain its signature, but its inputs cannot distinguish every RGB source or reproduce `Decoder`'s upsampling policy. P4-72 fixes both supported Decoder request routes; this item tracks deprecation/private-internals vs a metadata-aware companion API. |
| P4-81 | [Linux cdylib omits GNU `LIBJPEG_8.0` symbol versions](last_mile/phase4.md#p4-81-linux-cdylib-omits-gnu-libjpeg_80-symbol-versions--partial-nodes-emitted-and-tested-downstream-re-verification-pending) | 4 | PARTIAL | `LIBJPEG_8.0` + `LIBJPEGTURBO_8.0` are emitted on ELF targets, gated on the v8 SONAME, with no catch-all so the 63 TurboJPEG exports are not mislabelled as reference API. Remaining: re-run the OpenCV harness to confirm the loader warning is gone, plus the libtiff/GDAL/Poppler/HDF4 load check. |
| P4-84 | [Progressive/arithmetic classic scanline encoding still drops input smoothing](last_mile/phase4.md#p4-84-progressivearithmetic-classic-scanline-encoding-still-drops-input-smoothing--open) | 4 | OPEN | P4-83 fixes baseline smoothing byte-exactly, but SOF2/SOF9/SOF10 dispatch still bypasses the full-plane smoother. Implement the C behavior or reject visibly; never ignore it. |
| P4-85 | [Classic scanline compression ignores public custom quantization and Huffman tables](last_mile/phase4.md#p4-85-classic-scanline-compression-ignores-public-custom-quantization-and-huffman-tables--open) | 4 | OPEN | Native custom-table encoding works, but `run_encoder_and_flush` does not consume the classic C struct's quant/Huffman table slots; callers silently get default tables. |
| P4-86 | [Classic lossy scanline compression ignores public DCT method](last_mile/phase4.md#p4-86-classic-lossy-scanline-compression-ignores-public-dct-method--open) | 4 | OPEN | `run_encoder_and_flush` hardcodes ISLOW, so classic `JDCT_IFAST`/`JDCT_FLOAT` requests are silently ignored in every lossy mode. |
| P4-87 | [Classic abbreviated-datastream table state is not wired](last_mile/phase4.md#p4-87-classic-abbreviated-datastream-table-state-is-not-wired--open) | 4 | OPEN | `write_all_tables`, suppression/sent state, and installed-table-aware `jpeg_write_tables` are not connected across classic cinfo reuse. |
| P4-88 | [Classic scanline marker controls and CCIR601 rejection are ignored](last_mile/phase4.md#p4-88-classic-scanline-marker-controls-and-ccir601-rejection-are-ignored--open) | 4 | OPEN | JFIF/Adobe marker fields are ignored; unsupported CCIR601 sampling is accepted instead of raising the C error. |
| P4-89 | [Classic arithmetic+lossless requests silently become Huffman lossless](last_mile/phase4.md#p4-89-classic-arithmeticlossless-requests-silently-become-huffman-lossless--open) | 4 | OPEN | Upstream raises `JERR_ARITH_NOTIMPL`; lossless-first shim dispatch emits SOF3 and discards `arith_code`. |
| P4-90 | [Classic arithmetic scanline compression ignores public DAC conditioning](last_mile/phase4.md#p4-90-classic-arithmetic-scanline-compression-ignores-public-dac-conditioning--open) | 4 | OPEN | Public `arith_dc_L/U` and `arith_ac_K` slots are never forwarded to sequential/progressive arithmetic encoders. |
| P4-91 | [Classic scanline compression ignores custom scan scripts](last_mile/phase4.md#p4-91-classic-scanline-compression-ignores-custom-scan-scripts--open) | 4 | OPEN | Public `scan_info`/`num_scans` are never read; callers receive the fixed default progressive script. |
| P4-92 | [Classic scanline compression collapses valid sampling-factor layouts to 4:4:4](last_mile/phase4.md#p4-92-classic-scanline-compression-collapses-valid-sampling-factor-layouts-to-444--open) | 4 | OPEN | Luma-only enum inference maps S410/S24 and valid non-standard component layouts to S444. |
| P4-93 | [Classic scanline compression ignores requested JPEG colorspace](last_mile/phase4.md#p4-93-classic-scanline-compression-ignores-requested-jpeg-colorspace--open) | 4 | OPEN | `jpeg_color_space` is never read; RGB-direct requests become YCbCr and direct YCbCr input is reconverted as RGB. |
| P4-94 | [Classic 12/16-bit scanline buffers never reach a high-precision encoder](last_mile/phase4.md#p4-94-classic-1216-bit-scanline-buffers-never-reach-a-high-precision-encoder--open) | 4 | OPEN | High-precision writers fill `pixels_u16`, but finish always reads the cleared `pixels_u8`; the existing test was null-only. |
| P4-95 | [Classic raw-data compression drops most public encode options](last_mile/phase4.md#p4-95-classic-raw-data-compression-drops-most-public-encode-options--open) | 4 | OPEN | Raw planes encode, but restart/mode/table/script/colorspace and exact-sampling controls are discarded. |
| P4-96 | [Classic decompression color quantization and colormap switching are not wired](last_mile/phase4.md#p4-96-classic-decompression-color-quantization-and-colormap-switching-are-not-wired--open) | 4 | OPEN | Quantization flags only change row sizing; no palette/index output is produced, and `jpeg_new_colormap` is a no-op. |
| P4-97 | [`jpeg_resync_to_restart` is an unconditional success no-op](last_mile/phase4.md#p4-97-jpeg_resync_to_restart-is-an-unconditional-success-no-op--open) | 4 | OPEN | The exported/default callback returns TRUE without marker-state recovery, warning, refill, or suspension semantics. |
| P4-98 | [Classic 12/16-bit decode bypasses lifecycle and public output options](last_mile/phase4.md#p4-98-classic-1216-bit-decode-bypasses-lifecycle-and-public-output-options--open) | 4 | OPEN | High-precision reads decode directly after header and ignore colorspace/scaling/DCT/upsampling/quantization controls. |
| P4-99 | [Classic decode dispatcher ignores output options and colorspace metadata](last_mile/phase4.md#p4-99-classic-decode-dispatcher-ignores-output-options-and-colorspace-metadata--open) | 4 | OPEN | Scaling fields can be replaced by full-size output (buffer risk); colorspace fallback and missing APP14 metadata also erase caller/source intent. |
| P4-100 | [Classic codec failures are reported as suspension or silent success](last_mile/phase4.md#p4-100-classic-codec-failures-are-reported-as-suspension-or-silent-success--partial-translator--finishstart-entry-points-landed-batch-continues) | 4 | PARTIAL | Shared native→classic translator landed with exactly-one-`error_exit`; `jpeg_start_decompress` no longer reports failure as suspension and `jpeg_finish_compress` no longer resets to a success state or accepts short input. Remaining: the other private-string-only sites, the setjmp harness matrix, and the P4-104 state work it depends on. |
| P4-101 | [Classic header parse does not publish coding tables or scan state](last_mile/phase4.md#p4-101-classic-header-parse-does-not-publish-coding-tables-or-scan-state--open) | 4 | OPEN | DQT/DHT/DAC/DRI/SOS fields stay default; `is_baseline` also mislabels SOF1/SOF9. |
| P4-102 | [Classic raw-data decode bypasses public options and state contracts](last_mile/phase4.md#p4-102-classic-raw-data-decode-bypasses-public-options-and-state-contracts--open) | 4 | OPEN | Native full-image helpers bypass applicable options, suspension, state validation, and classic errors. |
| P4-103 | [`jpeg_crop_scanline` lacks iMCU-aligned C semantics](last_mile/phase4.md#p4-103-jpeg_crop_scanline-does-not-implement-imcu-aligned-c-semantics--open) | 4 | OPEN | Exact RGB slicing replaces upstream alignment, geometry updates, and state validation. |
| P4-104 | [Classic decompressor state constants/transitions and finish lifecycle diverge](last_mile/phase4.md#p4-104-classic-decompressor-state-constants-transitions-and-finish-lifecycle-diverge--open) | 4 | OPEN | STOPPING is misnumbered; header/finish/reset/suspension/term-source behavior diverges. |
| P4-105 | [Classic marker writers ignore state and declared lengths](last_mile/phase4.md#p4-105-classic-marker-writers-ignore-state-and-declared-lengths--open) | 4 | OPEN | Invalid timing and incomplete/overlong piecemeal markers are accepted/reordered. |
| P4-106 | [`jpeg_finish_compress` accepts incomplete input and bad states](last_mile/phase4.md#p4-106-jpeg_finish_compress-accepts-incomplete-input-and-bad-states--open) | 4 | OPEN | Missing rows become zero-filled output; helper errors are erased by reset. |
| P4-107 | [`jpeg_enable_lossless` clamps invalid input and omits public state](last_mile/phase4.md#p4-107-jpeg_enable_lossless-clamps-invalid-input-and-omits-public-state--open) | 4 | OPEN | Invalid predictor/Pt values are normalized instead of rejected; Ss/Se/Ah/Al stay stale. |
| P4-109 | [Classic source-manager setup and stdio semantics diverge](last_mile/phase4.md#p4-109-classic-source-manager-setup-and-stdio-semantics-diverge--open) | 4 | OPEN | Null/empty/foreign managers, FILE buffering, errors, and Windows differ. |
| P4-110 | [`jpeg_Create*` ignores version and struct-size ABI guards](last_mile/phase4.md#p4-110-jpeg_create-ignores-version-and-struct-size-abi-guards--open) | 4 | OPEN | **P0:** full Rust mirrors are written regardless of caller-declared ABI size. |
| P4-111 | [Classic progress-manager callbacks and counters are not wired](last_mile/phase4.md#p4-111-classic-progress-manager-callbacks-and-counters-are-not-wired--open) | 4 | OPEN | Native listeners do not implement `cinfo->progress`. |
| P4-112 | [`jpeg_set_marker_processor` callbacks are stored but never invoked](last_mile/phase4.md#p4-112-jpeg_set_marker_processor-callbacks-are-stored-but-never-invoked--open) | 4 | OPEN | Marker saving is enabled, but the C routine never receives source bytes or suspension. |
| P4-113 | [`jpeg_read_icc_profile` bypasses classic saved-marker semantics](last_mile/phase4.md#p4-113-jpeg_read_icc_profile-bypasses-classic-saved-marker-semantics--open) | 4 | OPEN | It reads a post-start native image instead of header-time `marker_list`. |
| P4-114 | [`jpeg_has_multiple_scans` equates multi-scan with progressive](last_mile/phase4.md#p4-114-jpeg_has_multiple_scans-equates-multi-scan-with-progressive--open) | 4 | OPEN | Sequential multi-scan streams return false; state validation is absent. |
| P4-115 | [Native 12-bit coverage claims include untested modes/layouts](last_mile/phase4.md#p4-115-native-12-bit-coverage-claims-include-untested-modes-and-sampling-layouts--open) | 4 | OPEN | Raw C matrix is six baseline layouts; S410/S24 raw-planar cases and progressive/arithmetic coverage remain missing. |
| P4-116 | [C-parity tests can turn failures or missing comparisons into a pass](last_mile/phase4.md#p4-116-c-parity-tests-can-convert-failures-or-missing-comparisons-into-a-pass--partial-named-matrices-closed-repository-wide-sweep-remains) | 4 | PARTIAL | All ten named suites now fail closed; seven assert exact planned-vs-executed counts. The tables-only matrix had been comparing **zero** cases against a `cjpeg` switch that does not exist, and the 14,112-case full transform matrix counted refusals into a variable it only printed. Remaining: 82 test files still contain `SKIP` sites, and ~9 sampled suites outside the named ten turn a failed C invocation into a skip. |
| P4-120 | [classic-shim allocation-failure paths are unreachable from tests](last_mile/phase4.md#p4-120-classic-shim-allocation-failure-paths-are-unreachable-from-tests--open) | 4 | OPEN | `JERR_OUT_OF_MEMORY` (code 56, and its `ERREXIT1` `msg_parm` payload) is emitted by two `jpeg_mem_dest` paths that no test can force. Filed in the P4-108 review; fits P4-116's test-integrity scope. |
| P4-121 | [lossless encode accepts a restart interval C refuses to decode](last_mile/phase4.md#p4-121-lossless-encode-accepts-a-restart-interval-c-refuses-to-decode--open) | 4 | OPEN | `jclossls.c:294-296` raises `JERR_BAD_RESTART` when `Ri % MCUs_per_row != 0`; we emit it, producing a stream `djpeg` rejects. Found while repairing the P4-116 lossless cross-check. |
| P2-G | [Downstream lab multi-version / multi-distro matrix](last_mile/backlog.md#p2-g-downstream-lab-multi-version--multi-distro-matrix--open) | Backlog | OPEN | The pinned OpenCV 4.6/Ubuntu 24.04 leg is complete; Qt5/Qt6, additional OpenCV/distro versions, immutable scheduled environments, and broader version pairs remain. |

**Next up:** close the remaining P0, the create-size overwrite risk **P4-110**. **P4-116** is PARTIAL — its ten named matrices are closed; the repository-wide sweep remains. Then unify errors/lifecycle through **P4-100/P4-104/P4-106**. After those safety/correctness blockers, close ELF versioning **P4-81**, work through **P4-84..P4-115**, and expand **[P2-G](last_mile/backlog.md#p2-g-downstream-lab-multi-version--multi-distro-matrix--open)**. P4-108 destination ownership, P4-82 restart settings, and baseline-only P4-83 smoothing are closed; progressive/arithmetic smoothing remains P4-84.

---
## Phase Map

Each phase file is self-contained. Read only the one you need.

| Phase | File | Scope | Status |
| --- | --- | --- | --- |
| **Phase 1** | [last_mile/phase1.md](last_mile/phase1.md) | Original release gate: P0-1..4, P1 (Soft-Skip / Encode SIMD / Legacy / Precision / YUV), Phase-1 P2 (tjbench / PNG), Execution Plan (Tasks 1-7), Definition of Done. | All CLOSED — historical reference. |
| **Phase 2** | [last_mile/phase2.md](last_mile/phase2.md) | System-library drop-in hardening: P2-1..11 (workflow flags, printf expansion, ABI cross-check, symbol inventory, install layout, fuzzing, distro consumers, progressive-encode samp411, crates.io publish). | All CLOSED. |
| **Phase 3** | [last_mile/phase3.md](last_mile/phase3.md) | Long-tail C compatibility: P3-1 (ABI offset cross-check), P3-2 (12-bit raw-data backend), P3-3 (symbol-inventory allowlist triage), P3-4 (4-pixel chroma transform gate), P3-5 (classic lifecycle harness), P3-6 (non-standard sampling / RGB565). | All CLOSED. |
| **Phase 4** | [last_mile/phase4.md](last_mile/phase4.md) | Post-gate corrections after Phase 3, including classic C-ABI and test-integrity audits. | P4-40/82/83/119 CLOSED; P4-13 PARTIAL; remaining OPEN/PARTIAL set is listed above. |
| **Backlog** | [last_mile/backlog.md](last_mile/backlog.md) | Deferred replacement coverage with explicit in-repository scope and acceptance criteria; currently P2-G downstream multi-version/multi-distro lab. | P2-G OPEN; first OpenCV leg complete 2026-08-02. |
| **Reference** | [last_mile/reference_commands.md](last_mile/reference_commands.md) | Common verification commands (workspace test, stock-tool build, encode bench matrix, etc.). | — |

---

The Phase 3 Suggested Order lived here; every entry is CLOSED and the detail
is in [last_mile/phase3.md](last_mile/phase3.md). Removed from the index to
keep it under the ~150-line cap.

---

## Maintenance Protocol

The mechanics for adding/closing/archiving entries are documented in the project root `CLAUDE.md` under "LAST_MILE Management" — read that before editing any of these files.
